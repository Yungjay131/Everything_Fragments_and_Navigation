<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".WelcomeActivity"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    android:id="@+id/drawer_welcome">

    <include
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        layout="@layout/app_bar_welcome"/>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_welcome"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:itemHorizontalPadding="40dp"
        app:itemTextColor="@color/appBlue"
        app:itemBackground="@drawable/menu_item_padding_welcome"
        app:headerLayout="@layout/nav_header_welcome"
        app:menu="@menu/menu_welcome"/>

</androidx.drawerlayout.widget.DrawerLayout>